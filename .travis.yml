os: osx
osx_image: xcode11.3
language: ruby
script:
  - brew install iterative/homebrew-dvc/dvc
  - dvc version
  - brew linkage dvc
  - brew test dvc
  - brew audit --strict dvc
  - brew uninstall dvc
